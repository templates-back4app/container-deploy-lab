# Copy to .env for local runs; on Back4app Containers set these under Settings → Environment.
# main needs nothing. The platform sets PORT itself; 8080 is the fallback in server.js.
PORT=8080
# This branch refuses to boot without it. Any non-empty value passes the check.
REQUIRED_SECRET=change-me
